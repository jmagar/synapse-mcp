# homelab-mcp-server Environment Configuration
# Copy this file to .env and customize as needed

# =============================================================================
# HTTP Server (only used with --http transport mode)
# =============================================================================

# Server port (default: 3000)
HOMELAB_PORT=3000

# Server host/bind address (default: 127.0.0.1)
# Use 0.0.0.0 to listen on all interfaces
HOMELAB_HOST=127.0.0.1

# =============================================================================
# Host Configuration
# =============================================================================

# Explicit path to config file (optional)
# If not set, searches: ./homelab.config.json, ~/.config/homelab-mcp/config.json, ~/.homelab-mcp.json
# HOMELAB_CONFIG_FILE=/path/to/homelab.config.json

# JSON array of host configurations (fallback if no config file found)
# HOMELAB_HOSTS_CONFIG='[{"name":"unraid","host":"unraid.local","port":2375,"protocol":"http"},{"name":"proxmox","host":"proxmox.local","port":2375,"protocol":"http"}]'

# Default host to use when no host is specified in requests
# HOMELAB_DEFAULT_HOST=unraid

# =============================================================================
# SSH Connection Pool
# =============================================================================

# Maximum concurrent SSH connections per host (default: 5)
HOMELAB_SSH_MAX_CONNECTIONS=5

# Idle connection timeout in milliseconds (default: 60000 = 1 minute)
HOMELAB_SSH_IDLE_TIMEOUT_MS=60000

# SSH connection timeout in milliseconds (default: 5000 = 5 seconds)
HOMELAB_SSH_CONNECTION_TIMEOUT_MS=5000

# Enable periodic health checks on idle connections (default: true)
# Set to "false" to disable
HOMELAB_SSH_ENABLE_HEALTH_CHECKS=true

# Health check interval in milliseconds (default: 30000 = 30 seconds)
HOMELAB_SSH_HEALTH_CHECK_INTERVAL_MS=30000
